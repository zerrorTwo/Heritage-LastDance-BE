# Production Configuration
PORT: 3000

# Database
PORT_DB: 5432

# JWT
JWT_SECRET: "change_me_production_jwt_secret"
JWT_EXPIRES_IN: "15m"
JWT_REFRESH_EXPIRES_DAYS: 30

# Session
SESSION_TTL_HOURS: 24
REFRESH_TOKEN_TTL_DAYS: 7

# OTP
OTP_EXPIRE_MINUTES: 5
OTP_RESEND_LIMIT_TTL_MINUTES: 60
OTP_MAX_ATTEMPTS_PER_HOUR: 5
OTP_MAX_RESEND_ATTEMPTS: 5

# Reset Password
RESET_PASSWORD_TOKEN_TTL_MINUTES: 15

# MetaMask
METAMASK_CHALLENGE_TTL_MINUTES: 2

# Frontend
FRONTEND_URL: "https://your-production-domain.com"

# Google OAuth
GOOGLE_CLIENT_ID: ""
GOOGLE_CLIENT_SECRET: ""
GOOGLE_CALLBACK_URL: "https://your-production-domain.com/v1/api/auth/google/callback"

# Throttling
THROTTLE_TTL: 60
THROTTLE_LIMIT: 100

# Resend
RESEND_API_KEY: ""
RESEND_FROM: "AIOZ STREAM <no-reply@yourdomain.com>"

# Cloudinary
CLOUDINARY_CLOUD_NAME: ""
CLOUDINARY_API_KEY: ""
CLOUDINARY_API_SECRET: ""
